## Notes
- Do NOT share credentials in chat. Use Replit Secrets.
- quotex_client uses Playwright + WebSocket approach; update parser if Quotex changes site.
- For parsing issues paste sanitized WS lines here (no credentials) and I will adapt parser.
